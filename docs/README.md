# docs / media

Drop a demo image here named `echo-editor.png` (and/or `echo-demo.gif`), then
uncomment the image line near the top of the root `README.md`:

```markdown
![Echo editor](docs/echo-editor.png)
```

**What reads best** (from experience): the **editor** with a few real steps
captured — thumbnails, descriptions, the accept/reject diff affordance — not
the popup. If you record a demo flow, use a throwaway/sample site so nothing
sensitive ends up in the screenshots.
